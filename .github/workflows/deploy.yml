name: Build and Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install dependencies
        run: npm install

      - name: Build the app
        run: npm run build

      - name: LS
        run: ls

    #   - name: Copy build directory via SFTP
    #     uses: appleboy/sftp-action@master
    #     with:
    #       server: your-sftp-server.com
    #       username: ${{ secrets.SFTP_USERNAME }}
    #       password: ${{ secrets.SFTP_PASSWORD }}
    #       port: 22
    #       local_path: build
    #       remote_path: /path/to/public_html
